import { Container } from '@mui/material';
import React, { FC } from 'react';
import Form from '../../components/form/Form';

const Auth: FC = () => {
  return (
    <Container>
      <Form />
    </Container>
  );
};

export default Auth;
